import LoginCard from "./components/loginCard";

export default function LoginPage() {
    return (
        <div className="bg-[url('/images/heroCardImage.png')] bg-cover bg-center w-full h-screen flex flex-col justify-center items-center">
           <LoginCard/>
        </div>
    );
}